<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cl_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/cl_item_icon"
        android:layout_width="@dimen/cl_item_icon"
        android:layout_height="@dimen/cl_item_icon"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:tint="@color/colorPrimary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/round" />

    <TextView
        android:id="@+id/cl_item_letter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ZZ"
        android:textAllCaps="true"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@+id/cl_item_icon"
        app:layout_constraintEnd_toEndOf="@+id/cl_item_icon"
        app:layout_constraintStart_toStartOf="@+id/cl_item_icon"
        app:layout_constraintTop_toTopOf="@+id/cl_item_icon" />

    <TextView
        android:id="@+id/cl_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:text="TextView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/cl_item_status"
        app:layout_constraintStart_toEndOf="@+id/cl_item_icon"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/cl_item_status"
        android:layout_width="@dimen/cl_item_status_icon"
        android:layout_height="@dimen/cl_item_status_icon"
        android:layout_marginEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_cloud_upload" />
</androidx.constraintlayout.widget.ConstraintLayout>